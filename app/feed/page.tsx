import { PageStub } from "@/components/shared/PageStub";

export const metadata = { title: "Trip Feed — Wander Women" };

export default function FeedPage() {
  return (
    <PageStub
      title="Trip Feed"
      step="A.9"
      description="Real itineraries with rupee + USD costs. Receipts, not inspiration."
    />
  );
}
