import { PageStub } from "@/components/shared/PageStub";

export const metadata = { title: "Find a Buddy — Wander Women" };

export default function BuddyPage() {
  return (
    <PageStub
      title="Find a Buddy"
      step="A.9"
      description="Match with women going to the same destination on overlapping dates. Three large match cards with overlap visualization."
    />
  );
}
