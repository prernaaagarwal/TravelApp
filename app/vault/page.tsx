import { PageStub } from "@/components/shared/PageStub";

export const metadata = { title: "WhatsApp Trip Vault — Wander Women" };

export default function VaultPage() {
  return (
    <PageStub
      title="WhatsApp Trip Vault"
      step="A.9"
      description="Your trip docs, bookings, and emergency contacts — accessible by WhatsApp. ₹199 per trip. Mock conversation preview, no real Twilio in V0."
    />
  );
}
