import { PageStub } from "@/components/shared/PageStub";

export const metadata = { title: "Explore — Wander Women" };

export default function ExplorePage() {
  return (
    <PageStub
      title="Browse all Trip Intel"
      step="A.7"
      description="A scrollable grid of every Trip Intel Card we have, filterable by India / International / For Foreign Women."
    />
  );
}
