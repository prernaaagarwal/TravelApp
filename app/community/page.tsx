import { PageStub } from "@/components/shared/PageStub";

export const metadata = { title: "Community — Wander Women" };

export default function CommunityPage() {
  return (
    <PageStub
      title="Community Hub"
      step="A.8"
      description="Four tabs: Ask the community, Ask a local sister, Rant space, and the Beware Board — every post seeded by real solo female travelers."
    />
  );
}
