/**
 * Copy for every Beware board Leaflet map ( /community/beware/[city] ).
 * New map surfaces should import this so the hint stays consistent.
 */
export const BEWARE_MAP_REPORTS_STATS_HINT =
  "Click below to see the reports ⬇️" as const;
